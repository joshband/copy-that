# DEPRECATED — Playwright leftover folder

**Canonical E2E home:** [`../tests/playwright/`](../tests/playwright/README.md)

This directory is a **stub only**. Orphaned overview/metrics/relations specs and
legacy docs were removed 2026-09-20 (they were never on `pnpm test:e2e`
`testDir`). Granular overview coverage that still matters lives under
`frontend/tests/playwright/` (`summary_chips`, `diagnostics-panel`,
`tabs_layout`, `mvp-phase1-smoke`).

Do **not** add new tests here. Run E2E via `pnpm test:e2e` from the repo root.

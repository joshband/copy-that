# DEPRECATED — Playwright leftover folder

**Canonical E2E home:** [`../../frontend/tests/playwright/`](../../frontend/tests/playwright/README.md)

This directory is a **stub only**. Legacy soft shadow smoke specs were removed
2026-09-20 (superseded by `frontend/tests/playwright/shadow-tokens.spec.ts`
and never on the JS `testDir`).

Do **not** add new tests here. Prefer `pnpm test:e2e` from the repo root.
Python UI E2E (`tests/ui/`) is a separate stack — do not confuse the two.

/**
 * React Theme Object
 * Generated by copy-that
 */

{% macro render_object(tree, indent=1) %}
{% set space = "  " * indent %}
{% for key, value in tree.items() %}
{% if value is mapping and "$value" in value %}
{{ space }}{{ key }}: {% if value["$value"] is string %}"{{ value["$value"] }}"{% else %}{{ value["$value"] | tojson }}{% endif %},
{% else %}
{{ space }}{{ key }}: {
{{ render_object(value, indent + 1) }}
{{ space }}},
{% endif %}
{% endfor %}
{% endmacro %}
export const tokens = {
{{ render_object(token_tree) }}
} as const;

export type Tokens = typeof tokens;

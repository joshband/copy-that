# API Examples (curl)

_Add endpoint-by-endpoint curl examples with request/response bodies for core routes: projects CRUD, color extraction (URL/base64), sessions/libraries (create, batch extract, curate, export). Include auth/CORS notes if applicable._
